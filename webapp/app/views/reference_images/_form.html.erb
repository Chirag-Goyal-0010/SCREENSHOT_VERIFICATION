<%= form_with(model: reference_image, local: true, multipart: true) do |form| %>
  <% if reference_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reference_image.errors.count, "error") %> prohibited this reference_image from being saved:</h2>

      <ul>
        <% reference_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :file, "Upload Image", style: "display: block" %>
    <%= form.file_field :file %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
